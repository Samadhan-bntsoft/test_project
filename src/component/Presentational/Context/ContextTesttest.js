// import React from "react";
// import { act, render, fireEvent, cleanup } from "@testing-library/react";
// import ContextTest from "./ContextTest.js";

// afterEach(cleanup);

// describe("context test checking", () => {
//   it("Context value is updated by child component", () => {
//     const { container, getByText } = render(<ContextTest />);
//     expect(getByText(/some/i).textContent).toBe("some");
//     fireEvent.click(getByText("Change Text"));
//     expect(getByText(/some/i).textContent).toBe("some Text change");
//   });
// });

